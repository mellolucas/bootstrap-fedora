user: "{{ ansible_user_id | lower }}"
system: "{{ ansible_system | lower }}"
arch: "{{ ansible_architecture | lower }}"
distribution:
  name: "{{ ansible_distribution | lower }}"
  version: "{{ ansible_distribution_major_version | lower }}"
